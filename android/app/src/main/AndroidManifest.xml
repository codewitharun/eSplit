<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools"
    package="com.techtitens.ezysplit">

    <uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.READ_MEDIA_DOCUMENTS" />

<uses-permission android:name="com.google.android.gms.permission.AD_ID" />



<queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
    <data android:mimeType="application/pdf" />
        </intent>
    </queries>
    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher"
      android:allowBackup="false"
      android:theme="@style/AppTheme">

      <!-- Adding @react-native-firebase/analytics pulls in
           play-services-measurement-api, which declares its own
           AD_SERVICES_CONFIG property pointing at ga_ad_services_config -
           this collides with react-native-google-mobile-ads'
           (play-services-ads-lite) identical property pointing at
           gma_ad_services_config, and the manifest merger refuses to pick
           one on its own. Explicitly keeping GMA's file is safe: both
           files just grant the same allow-all attribution access, and
           GMA's also covers Topics/Custom Audiences, so nothing Firebase
           Analytics needs is lost. Revisit only if either SDK moves off
           allowAllToAccess to per-partner enrollment IDs, since then the
           two files would need merging instead of one replacing the
           other. See: https://github.com/invertase/react-native-google-mobile-ads/issues/657 -->
      <property
          android:name="android.adservices.AD_SERVICES_CONFIG"
          android:resource="@xml/gma_ad_services_config"
          tools:replace="android:resource" />

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter  android:autoVerify="true">
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <intent-filter>
    <action android:name="android.intent.action.VIEW" />
    
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />
    <data android:scheme="ezysplit"  />
</intent-filter>
   
<intent-filter android:autoVerify="true">
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />

    <!-- HTTPS support -->
    <data
        android:scheme="https"
        android:host="ezysplit.arun.codes"
        android:pathPrefix="/app" />

    <!-- HTTP support -->
    <data
        android:scheme="http"
        android:host="ezysplit.arun.codes"
        android:pathPrefix="/app" />
</intent-filter>


      </activity>
    </application>
</manifest>
